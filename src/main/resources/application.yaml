spring:
  application:
    name: store
  datasource:
    url: jdbc:postgresql://localhost:5432/store
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    # hibernate:
    #   ddl-auto: none
